import React from 'react'
import ReactDOM from 'react-dom/client'
import TailwindApp from './TailwindApp.tsx'
import './index.css'

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <TailwindApp />
  </React.StrictMode>,
)
